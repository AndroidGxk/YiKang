<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.yikangcheng.admin.yikang">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".app.BaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.aftersale.AfterSaleActivity"></activity>
        <activity
            android:name=".activity.LoginActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.RegisterActivity"
            android:launchMode="singleTask" />
        <activity android:name=".activity.myaccount.MyaccountActivity" />
        <activity android:name=".activity.seek.SeekActivity" />
        <activity android:name=".activity.particulars.ParticularsActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="design_width_in_dp"
            android:value="360" />
        <meta-data
            android:name="design_height_in_dp"
            android:value="640" />

        <activity android:name=".activity.SeckillSecondActivity" />
        <activity
            android:name=".activity.particulars.ParticularsActivity"
            tools:ignore="DuplicateActivity" />
        <activity
            android:name=".activity.seek.SeekActivity"
            tools:ignore="DuplicateActivity" />
        <activity android:name=".activity.SearchListActivity" />
        <activity android:name=".activity.aftersale.AfterSaleActivity"
            tools:ignore="DuplicateActivity" />
    </application>

</manifest>